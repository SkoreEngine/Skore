# Cppcheck suppressions for Skore (used by scripts/cppcheck.sh and CI).
# Format: errorId:file:line (wildcards allowed; see cppcheck --doc)
# A lone "#" is invalid in cppcheck 2.13 (comment lines must be longer than 1).
# Vendored / generated paths (also filtered with -i in the script).
*:thirdparty/*
*:*/thirdparty/*
*:build/*
*:*/build/*
*:cmake-build-*/*
# System headers / missing platform SDKs on some runners.
missingIncludeSystem
# Noise that fights this engine's C style (opaque handles, API tables, padding,
# ABI mirror structs, trust-the-caller null contracts). See AGENTS.md.
constVariablePointer
constParameterPointer
constParameterCallback
constParameter
nullPointerRedundantCheck
unusedStructMember
badBitmaskCheck
variableScope
unmatchedSuppression
# Known false positives (branch analysis / buffer confusion).
uninitvar:app/filesystem_unix.c
arrayIndexOutOfBoundsCond:app/platform_unix.c
# jolt plugin test helper: cppcheck cannot see that the C++ body_create
# implementation consumes the shape description synchronously, so it models
# the call as retaining the caller's local desc (opaque API boundary).
returnDanglingLifetime:plugins/jolt/jolt_tests.c
