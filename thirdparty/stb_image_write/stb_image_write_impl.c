/**
 * @file stb_image_write_impl.c
 * @brief Single TU that compiles stb_image_write (Sean Barrett / nothings).
 *
 * Consumers include stb_image_write.h and link the stb_image_write static lib.
 * STB_IMAGE_WRITE_IMPLEMENTATION is defined exactly once here.
 */

#define STB_IMAGE_WRITE_IMPLEMENTATION
#include "stb_image_write.h"
